# Declarative Routing with Flat Routes

Using React Router's declarative flat-file routing via `app/routes.ts`. Routes defined as config, not file system.

Enables explicit route ordering, nested layouts without folder nesting, and cleaner route-to-component mapping.
