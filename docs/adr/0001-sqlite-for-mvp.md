# SQLite for MVP

Using SQLite with in-memory storage for development. Chosen for speed of setup and zero-configuration local development.

SQLite chosen over PostgreSQL for MVP to eliminate database provisioning overhead. The schema is designed to be compatible with PostgreSQL when migration becomes necessary.
